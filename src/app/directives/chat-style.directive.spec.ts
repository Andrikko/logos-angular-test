import { ChatStyleDirective } from './chat-style.directive';

describe('ChatStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new ChatStyleDirective();
    expect(directive).toBeTruthy();
  });
});
