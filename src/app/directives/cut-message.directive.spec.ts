import { CutMessageDirective } from './cut-message.directive';

describe('CutMessageDirective', () => {
  it('should create an instance', () => {
    const directive = new CutMessageDirective();
    expect(directive).toBeTruthy();
  });
});
